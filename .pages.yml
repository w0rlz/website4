media:
  - name: default
    input: images
    output: images

content:
  - name: Baseball Photos
    type: collection
    path: images/baseball
    media_folder: /images/baseball  # <--- Add this
    public_folder: /images/baseball # <--- Add this so the site can find it
    fields:
      - name: photo
        label: Upload Photo
        type: image
      - name: date
        label: Date Taken
        type: date
      - name: caption
        label: What is this about?
        type: string
  - name: Aviation Photos
    type: collection
    path: images/aviation
    media_folder: /images/aviation  # <--- Add this
    public_folder: /images/aviation # <--- Add this so the site can find it
    fields:
      - name: photo
        label: Upload Photo
        type: image
      - name: date
        label: Date Taken
        type: date
      - name: caption
        label: What is this about?
        type: string
  - name: Misc Photos
    type: collection
    path: images/misc
    media_folder: /images/misc  # <--- Add this
    public_folder: /images/misc # <--- Add this so the site can find it
    fields:
      - name: photo
        label: Upload Photo
        type: image
      - name: date
        label: Date Taken
        type: date
      - name: caption
        label: What is this about?
        type: string